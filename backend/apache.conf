<VirtualHost *:80>
    ServerName localhost
    DocumentRoot /var/www/html/public

    <Directory /var/www/html/public>
        AllowOverride None
        Order Allow,Deny
        Allow from All
        Require all granted

        FallbackResource /index.php

        # Pour éviter les erreurs avec .htaccess
        Options -MultiViews
    </Directory>

    # 🔥 AJOUT ESSENTIEL POUR JWT 🔥
    # Ce bloc permet à Apache de transmettre le header Authorization à PHP
    SetEnvIf Authorization "(.*)" HTTP_AUTHORIZATION=$1

    ErrorLog /var/log/apache2/error.log
    CustomLog /var/log/apache2/access.log combined
</VirtualHost>
